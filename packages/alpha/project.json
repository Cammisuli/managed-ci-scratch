{
  "name": "alpha",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('alpha build'); setTimeout(() => {}, 2000)\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('alpha test'); setTimeout(() => {}, 2000)\""
      }
    }
  }
}
