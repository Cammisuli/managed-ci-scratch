{
  "name": "beta",
  "implicitDependencies": ["alpha"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('beta build'); setTimeout(() => {}, 2000)\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('beta test'); setTimeout(() => {}, 2000)\""
      }
    }
  }
}
