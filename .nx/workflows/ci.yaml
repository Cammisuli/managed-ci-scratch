triggers:
  main-pushes:
    event: PUSH
    branches: [main]
  pull-requests:
    event: PR

workflows:
  main:
    runs-on: local-small
    steps:
      - name: Sleep (verify 5b - cancel while disabled)
        script: sleep 40
