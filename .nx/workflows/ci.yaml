triggers:
  main-pushes:
    event: PUSH
    branches: [main]
  pull-requests:
    event: PR

workflows:
  main:
    runs-on: local-small
    steps:
      - name: Sleep (verify 5c - completes naturally while disabled)
        script: sleep 15
